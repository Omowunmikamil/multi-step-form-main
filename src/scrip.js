/*
// Check if the page has been visited before
if (localStorage.getItem("visitedSummary")) {
  // Add the background color class if the page has been visited
  document.getElementById("summaryButton").classList.add("bg-p-lightBlue");
}

// Store the page visit state when the page is loaded
window.addEventListener("load", function () {
  // Save the visited state in localStorage
  localStorage.setItem("visitedSummary", "true");
});*/
